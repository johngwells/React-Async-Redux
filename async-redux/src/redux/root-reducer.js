export const  INITIAL_STATE = {
  // initial state here
}

const useReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default useReducer;
